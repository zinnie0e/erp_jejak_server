<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guru.erpserver.mapper.dbsky.DbSkyTestMapper">
	<!-- dbskytest -->
	
	<!-- 전도서재고조회 -->
	<select id="selAllBooksStock4" resultType="dbSkyTestVO">
		SELECT sqcrnm0
		FROM KSCQNTY0 
		WHERE sqbook LIKE #{sbbook}
	</select>
</mapper>